version: 0.2
phases:
  build:
    commands:
      - echo "Building Docker image..."
      - docker build -t ayman380/intgra:latest .
  post_build:
    commands:
      - echo "Pushing Docker image..."
      - docker push ayman380/intgra:latest
